{
  "name": "swipe-keyboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery-browserify": "^1.8.1",
    "jquery-mousewheel": "^3.1.13",
    "simple-keyboard": "latest",
    "simple-keyboard-layouts": "latest"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.1",
    "gh-pages": "^2.2.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Alutery/moving-keyboard.git"
  }
}
